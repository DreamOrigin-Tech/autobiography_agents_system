# NOTE: LangGraph integration was stubbed out but never used.
# The system currently uses direct service-to-agent calls instead,
# which is simpler and more appropriate for the human-in-the-loop
# interview → write → edit flow.
#
# If full automation is needed in the future (e.g. auto-schedule
# interviews, auto-write after N rounds), re-enable this module.
