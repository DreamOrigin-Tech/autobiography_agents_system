# 优化 TODO

更新日期：2026-07-22

## 当前迭代：用户视角测试修复

- [x] **P1 统一本地访问地址**
  - `localhost:6985` 与 `127.0.0.1:6985` 都能正常加载项目。
  - 前端 API 地址自动跟随当前页面的本地主机名。
  - 验收：两个入口都能显示项目列表，不再停留在骨架屏。

- [x] **P1 为 API 加入超时和首页重试**
  - 请求长时间无响应时给出可理解的错误。
  - 首页显示“重新加载”操作，不无限展示骨架屏。
  - 验收：模拟 API 不可用时，页面能退出加载状态并允许重试。

- [x] **P1 统一采访与写作行动**
  - AI 仍在追问时，不突出“去写作”按钮。
  - 仅在采访 Agent 建议写作且素材充足时显示主要写作入口。
  - 验收：页面同一时刻只有一个明确的主要下一步。

- [x] **P2 尊重跳过和模糊记忆**
  - 用户说“不想说、跳过、不方便”后，不再显示“可以继续聊这一点”。
  - 用户说“记不清、想不起来”时不显示评分式补充提示。
  - 验收：边界回答后只保留低压力的 AI 追问。

- [x] **P2 优化语音失败回退**
  - 切回文字模式时清除麦克风错误。
  - 首次进入语音模式时说明会请求麦克风权限。
  - 验收：语音失败不影响后续文字采访，手机端控件不遮挡导航。

- [x] **P2 隔离自动化测试数据库**
  - pytest 使用临时 SQLite 数据库。
  - 测试完成后删除临时数据库文件。
  - 验收：运行测试不会再向 `backend/autobiography.db` 添加测试项目。

- [x] **最终验证**
  - 后端 pytest 全量通过。
  - 前端语音/API 单测、lint、TypeScript、生产构建通过。
  - 浏览器完成 `localhost`、`127.0.0.1`、桌面和手机尺寸复测。

## 后续储备

- [x] **P0 多用户登录与数据隔离**
  - 所有私有工作台接口要求登录，项目、章节、采访、修改和发布数据按用户隔离。
  - 支持微信开放平台网站应用扫码登录；未配置微信时可使用显式本地管理员回退。
  - 会话使用 HttpOnly Cookie，OAuth state 防重放，公开分享保持匿名可读。
  - 验收：后端 43 个测试通过，覆盖未登录、越权、退出、OAuth state 和微信账号复用。
- [ ] **P0 配置真实微信开放平台**
  - 在 `backend/.env` 填入已审核的网站应用 `WECHAT_APP_ID`、`WECHAT_APP_SECRET`。
  - 将公网回调地址配置为 `WECHAT_REDIRECT_URI`，并在微信开放平台登记回调域名。
  - 重启后端并完成一次真实扫码回调验收。
- [ ] 多用户后评估本地 embedding（如 sentence-transformers）。
- [ ] 商业产品阶段为 RAG 写作与全文检索引入向量库。
